<% content_for :page_title do %>
루비온레일스 가이드 
<% end %>

<% content_for :header_section do %>
<%= render 'welcome' %>
<% end %>

<% content_for :index_section do %>
<div id="subCol">
  <dl>
    <dd class="kindle">레일스 가이드는 Kindle과 <%= link_to 'Free Kindle Reading Apps', 'http://www.amazon.com/gp/kindle/kcp' %>에서도 이용할 수 있고 iPad,
iPhone, Mac, Android, 등은 <%= link_to '여기', @mobi %>에서 가이드를 다운로드할 수 있다.
    </dd>
    <dd class="work-in-progress">이 아이콘으로 표시된 가이드내용은 현재 작업 중에 있는 것이며 가이드 인덱스 메뉴에는 보이지 않는다. 유용한 내용들이지만 완벽하지 못해서 에러가 발생할 수도 있다. 내용을 검토해 보고 코멘트와 오류 수정내용을 게재하면 도움이 되겠다.</dd>
  </dl>
</div>
<% end %>

<% documents_by_section.each do |section| %>
  <h3><%= section['name'] %></h3>
  <dl>
    <% section['documents'].each do |document| %>
      <%= guide(document['name'], document['url'], :work_in_progress => document['work_in_progress']) do %>
        <p><%= document['description'] %></p>
      <% end %>
    <% end %>
  </dl>
<% end %>
